"""TensorFlow Day 9 -- Exercises. Run make_shapes.py first if you haven't
already. Fill in the TODOs, then run: python3 exercises.py
"""
import tensorflow as tf

# 1. Load shapes/ with image_dataset_from_directory using
#    validation_split=0.2 (matching seeds for both subsets).
# TODO

# 2. Build the Day 8 CNN architecture, compile() it, and train with
#    model.fit() for 15 epochs. Print history.history["accuracy"] vs
#    history.history["val_accuracy"] side by side.
# TODO

# 3. Reimplement the same training run with a custom loop instead
#    (train_one_epoch / evaluate functions) and confirm you get a
#    comparable final validation accuracy.
# TODO

# 4. Deliberately shrink the training set to just 2-3 images per class
#    and retrain -- watch train accuracy shoot to 100% while validation
#    accuracy stays low or noisy. This is overfitting, previewed here,
#    covered properly on Day 10.
# TODO
