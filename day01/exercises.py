"""TensorFlow Day 1 -- Exercises. Fill in the TODOs, then run: python3 exercises.py"""
import tensorflow as tf

# 1. Create a 1D tensor of the numbers 10 to 20 (inclusive) using
#    tf.range(), then print its shape and dtype.
# TODO

# 2. Create two 3x3 tensors of random numbers (tf.random.uniform). Compute
#    their element-wise product AND their matrix product. Print both, and
#    print the shapes to confirm they're different operations.
# TODO

# 3. Create a tensor `t = tf.range(24)` and reshape it into a (2, 3, 4)
#    tensor. Print its shape, then index into it to grab the single
#    number at position [1, 2, 3].
# TODO

# 4. Write device-agnostic code: check tf.config.list_physical_devices("GPU"),
#    build a device string ("/GPU:0" if a GPU is available else "/CPU:0"),
#    create a 3x3 random tensor inside a `with tf.device(device):` block,
#    and print which device string you used.
# TODO
